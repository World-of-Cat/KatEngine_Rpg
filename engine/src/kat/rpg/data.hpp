#pragma once

#include <unordered_map>
#include <any>
#include <filesystem>
#include <vector>

namespace kat::rpg {

    struct CustomPropertyTable {
        std::unordered_map<std::string, std::any> properties;
    };

    struct 
}